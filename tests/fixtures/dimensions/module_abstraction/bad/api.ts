export const A = 1;
export const B = 2;
export const C = 3;

export function wrapA(v: number): number {
  return v;
}

export function wrapB(v: number): number {
  return v;
}

export function wrapC(v: number): number {
  return v;
}
